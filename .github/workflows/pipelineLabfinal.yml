name : Workflow
on:
  push:
    branches:
      - featlabfinal

jobs:
  SAST:
    runs-on: ubuntu-latest
    steps:

    - name: Set up JDK 17
      uses: action/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Checkout code
      uses: action/checkout@v2

    - name: Log
      run: ls -R